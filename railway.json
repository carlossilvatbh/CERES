{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "cd backend && python manage.py collectstatic --noinput && python manage.py migrate && gunicorn ceres_project.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 60 --log-level info"
  }
}